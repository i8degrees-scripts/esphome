---

# IMPORTANT(JEFF): This is a matter of preference as to where
# we wish to pull our secrets from; Home Assistant -- 
# `/config/secrets.yaml` -- or ESPHome -- 
# `/config/esphome/secrets.yaml`.
#
# To use the default, i.e. ESPHome, comment the following line.
<<: !include ../secrets.yaml

